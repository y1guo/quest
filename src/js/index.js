const element = <h1>Quest</h1>;
// const element = React.createElement(
//     "button",
//     { onClick: () => this.setState({ liked: true }) },
//     "Like"
// );
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(element);
